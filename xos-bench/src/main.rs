//! XOS Bench — model validation harness.
//!
//! Measures tool-call schema success rate, the metric that decides whether a
//! local model can drive tools. The harness arrives in P1.

fn main() {
    println!("xos-bench {} — no cases loaded. The harness arrives in P1.", env!("CARGO_PKG_VERSION"));
}
